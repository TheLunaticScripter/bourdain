policy:
dircetory /tmp/foo created
dircetory /tmp/bar !created
dircetory /tmp/foo/baz created

state:
directory /tmp/foo !exists
directory /tmp/bar exists


existence?
  true
    NULL
  false
    repair

Order:
1. existence
2. parity to policy

check(file is created){
  true || false
}

# State                               
if (exec(resource.attribute.check){return}) {
  NULL
}
else {
  exec(resource.attribute.repair)
}

target.state(attribute.check).value